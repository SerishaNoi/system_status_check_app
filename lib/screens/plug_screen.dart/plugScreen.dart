import 'package:flutter/material.dart';

class PlugScreen extends StatelessWidget {
  const PlugScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Раздел находиться в разработке'),
    );
  }
}
